[project]
name = "findablex-worker"
version = "0.1.0"
description = "FindableX Celery Workers"
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
